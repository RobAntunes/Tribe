def test_function():    
    # This line is intentionally very long to trigger the linting functionality of our extension
    very_long_variable_name = "This is a very long string that should trigger the linting functionality of our extension because it exceeds 100 characters"
    
    # This line has trailing whitespace    
    
    # This function has inconsistent indentation
    inconsistent_indentation = True
    
    return very_long_variable_name 